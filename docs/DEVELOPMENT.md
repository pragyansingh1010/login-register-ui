# Development Notes

Keep form validation independent from visual state changes. Authentication demos should never hard-code real credentials or imply that client-side validation alone provides secure authentication.
